# Build-time default. Den faktiska PocketBase-URL:en väljs i RUNTIME utifrån
# MOVEXUM_ENV (staging|production) + de _STAGING/_PRODUCTION-suffixade
# Coolify-env-varsen (se apps/web/src/lib/pb-url.ts). URL:en läses server-side,
# så detta NEXT_PUBLIC-värde bakas inte in i någon klientkod som faktiskt
# anropar PocketBase — det är bara en ofarlig build-default (staging).
NEXT_PUBLIC_POCKETBASE_URL=http://pocketbase-r10nklch8dkune7s0flczb89.212.147.227.223.sslip.io

