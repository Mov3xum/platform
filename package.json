{
  "name": "platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@platform/web",
    "build": "npm run build --workspace=@platform/web",
    "start": "npm run start --workspace=@platform/web",
    "lint": "npm run lint --workspace=@platform/web"
  }
}
